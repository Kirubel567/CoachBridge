// Shared motion tokens for framer-motion.
// The tuple is explicitly typed as a 4-number cubic-bezier so TypeScript
// accepts it as an easing curve (bare array literals infer to number[]).
export const easeOutExpo: [number, number, number, number] = [0.16, 1, 0.3, 1];
